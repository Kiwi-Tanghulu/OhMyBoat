public enum GrabObjectType
{
    Stuff, 
    Equipment,
}

public enum InputMapType
{
    Play,
    MiniGame,
    Ship,
    UI,
    Port
}

public enum MissionType
{
    Sail,
    Leak,
}

public enum SailType
{
    Horizontal, 
    Vertical
}